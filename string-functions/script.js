let content = " Ece Sensoz - Ozgur Vurgun ";
let content_lowerCase = content.toLocaleLowerCase(); //localler bolgenin alfabesine gore islem yapar
let content_upperCase = content.toLocaleUpperCase();
let content_lenght = content.length;
let content_trim = content.trim();
let content_slice = content.slice(0, 3);
let content_split = content.split("-");
let content_index = content[2];
console.log(content_index);
